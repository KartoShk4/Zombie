/* ============================================
   КОНФИГУРАЦИЯ ИГРЫ
   ============================================
   Содержит все настройки игры: размеры мира,
   параметры игрока, зомби, пуль и волн.
   ============================================ */

// ===== РАЗМЕРЫ ИГРОВОГО МИРА =====
// Ширина и высота игрового мира в пикселях
const WORLD_WIDTH = 3000;
const WORLD_HEIGHT = 3000;

// ===== ОСНОВНАЯ КОНФИГУРАЦИЯ =====
const config = {
    // Параметры игрока
    player: {
        width: 40,      // Ширина игрока в пикселях (уменьшено)
        height: 40,     // Высота игрока в пикселях (уменьшено)
        speed: 5,        // Скорость движения игрока
        health: 100,     // Максимальное здоровье игрока
    },
    
    // Параметры пуль
    bullet: {
        radius: 3,      // Радиус пули в пикселях
        speed: 10,      // Скорость полета пули
        damage: 1,      // Урон от одной пули
        maxRange: 100   // Максимальная дальность стрельбы в пикселях (уменьшено)
    },
    
    // Параметры зомби
    zombie: {
        width: 30,              // Ширина зомби в пикселях
        height: 30,             // Высота зомби в пикселях
        speed: 0.4,             // Базовая скорость зомби
        health: 50,             // Базовое здоровье зомби
        damage: 5,               // Урон зомби за удар
        hitCooldown: 1.0        // Задержка между ударами (секунды)
    },
    
    // Параметры волн
    wave: {
        baseZombies: 5  // Базовое количество зомби в первой волне
    }
};




